{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "seed": "dotenv -- node prisma/seed.js",
    "dev": "nodemon index.js",
    "migrate:dev": "dotenv -e .env -- npx prisma migrate dev",
    "migrate:deploy": "dotenv -e .env -- npx prisma migrate deploy",
    "prisma:generate": "dotenv -e .env -- npx prisma generate",
    "prisma:studio": "dotenv -e .env -- npx prisma studio"
  },
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0",
    "express": "^5.1.0",
    "prisma": "^6.19.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
